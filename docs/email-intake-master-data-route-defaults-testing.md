# Testing note

Regression coverage added:

- sender mapping fills missing collection default;
- domain mapping fills missing collection and delivery defaults;
- parsed collection/destination are not overwritten;
- approved-order learning does not wipe route defaults;
- a single SQL route rule can fill missing collection/destination fields.