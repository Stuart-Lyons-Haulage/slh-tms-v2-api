# Production note

Expected production effect:

- new intake records should be less likely to show blank collection/destination when Master Data already knows the sender/default route;
- order documents handling is unchanged;
- inline signatures/images remain hidden from planner document lists;
- PDF/Excel/CSV evidence remains retained separately from route parsing.