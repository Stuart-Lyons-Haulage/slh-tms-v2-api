# Rollout note

Roll this out before replaying affected emails.

The replay is necessary because already-staged payload JSON will not automatically re-run the parser unless the intake replay path processes the original retained message again.